# @universal-vue/utilities

Small framework-safe helpers used by the public distributions, including class joining, clamping, browser detection, boolean normalization, and idempotent Custom Element registration.
