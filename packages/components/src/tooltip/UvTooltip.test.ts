import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import UvTooltip from'./UvTooltip.vue';describe('UvTooltip',()=>{it('connects trigger to tooltip',()=>{const wrapper=mount(UvTooltip,{props:{text:'Help'},slots:{default:'Trigger'}});const id=wrapper.get('[role=tooltip]').attributes('id');expect(wrapper.get('.uv-tooltip__trigger').attributes('aria-describedby')).toBe(id)})})
