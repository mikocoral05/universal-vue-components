import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import UvDialog from'./UvDialog.vue';describe('UvDialog',()=>{it('renders modal semantics and closes',async()=>{const wrapper=mount(UvDialog,{props:{modelValue:true,title:'Confirm'}});expect(wrapper.get('[role=dialog]').attributes('aria-modal')).toBe('true');await wrapper.get('button').trigger('click');expect(wrapper.emitted('update:modelValue')?.[0]).toEqual([false])})})
