import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import UvTabs from'./UvTabs.vue';const tabs=[{id:'one',label:'One'},{id:'two',label:'Two'}];describe('UvTabs',()=>{it('selects a tab',async()=>{const wrapper=mount(UvTabs,{props:{modelValue:'one',tabs}});await wrapper.findAll('[role=tab]')[1].trigger('click');expect(wrapper.emitted('update:modelValue')?.[0]).toEqual(['two'])})})
