import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import UvSelect from'./UvSelect.vue';describe('UvSelect',()=>{it('emits selected value',async()=>{const wrapper=mount(UvSelect,{props:{modelValue:'a',label:'Choice',options:[{label:'A',value:'a'},{label:'B',value:'b'}]}});await wrapper.get('select').setValue('b');expect(wrapper.emitted('update:modelValue')?.[0]).toEqual(['b'])})})
