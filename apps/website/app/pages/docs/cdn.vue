<script setup lang="ts">useSeoMeta({title:'CDN usage',description:'Use Universal Vue Components directly in the browser without a build step.'});const code=`<script src="https://cdn.jsdelivr.net/npm/@universal-vue/elements@0.1.0-alpha.1/dist/universal-vue.min.js">${'</'}script>

<uv-button variant="primary">Continue</uv-button>`</script>
<template><div class="page"><div class="site-container docs-layout"><DocsSidebar/><article class="prose"><span class="eyebrow">No build tools</span><h1>CDN bundle</h1><p>The browser-ready IIFE bundle auto-registers every element. It is served from the files published in the npm package.</p><CodeBlock :code="code" language="html"/><h2>Production guidance</h2><ul><li>Pin a real version instead of using <code>latest</code>.</li><li>Use subresource integrity when your deployment process supports it.</li><li>Prefer selective ESM registration for performance-sensitive applications.</li></ul><p>The CDN URL becomes valid only after the package owner publishes that version to npm.</p></article></div></div></template>
